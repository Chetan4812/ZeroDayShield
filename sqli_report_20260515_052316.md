# SQL Injection Vulnerability Report

**Scan target:** `/home/claude/sqli_zero_day/target_codebase`  
**Scan time:** 2026-05-15T05:23:16  
**Files scanned:** 1  
**Vulnerabilities found:** 3

---

## Summary

| ID | File | Line | Pattern | Severity | Confidence |
|----|------|------|---------|----------|------------|
| SQLI-001 | `app.py` | 30 | `string_concat` | 🔴 CRITICAL | 100% |
| SQLI-002 | `app.py` | 45 | `fstring` | 🟠 HIGH | 99% |
| SQLI-003 | `app.py` | 82 | `string_concat` | 🔴 CRITICAL | 100% |

---

## Findings

### SQLI-001 — 🔴 CRITICAL

**File:** `app.py` · **Line:** 30  
**Pattern:** `string_concat` · **Classifier confidence:** 100%

**Vulnerable code:**
```python
query = "SELECT * FROM users WHERE username = '" + username + "' AND password = '" + hashed + "'"
```

**Explanation:**
> Replaced string concatenation SQL with parameterized query. User-supplied variables ['username', 'hashed'] are now bound safely via '?' placeholders.

**Patched code:**
```python
db.execute("SELECT * FROM users WHERE username =  ?", (username, hashed))
```

**Diff:**
```diff
--- a/app.py:30
+++ b/app.py:30
@@ -1 +1 @@
-query = "SELECT * FROM users WHERE username = '" + username + "' AND password = '" + hashed + "'"

+db.execute("SELECT * FROM users WHERE username =  ?", (username, hashed))

```

---

### SQLI-002 — 🟠 HIGH

**File:** `app.py` · **Line:** 45  
**Pattern:** `fstring` · **Classifier confidence:** 99%

**Vulnerable code:**
```python
rows = db.execute(f"SELECT id, name, email FROM users WHERE name LIKE '%{term}%'").fetchall()
```

**Explanation:**
> Replaced f-string interpolation with parameterized query. Variables [] are now passed as bind parameters, with LIKE wildcards moved to the Python parameter.

**Patched code:**
```python
rows = db.execute("SELECT id, name, email FROM users WHERE name LIKE ", ()).fetchall()
```

**Diff:**
```diff
--- a/app.py:45
+++ b/app.py:45
@@ -1 +1 @@
-rows = db.execute(f"SELECT id, name, email FROM users WHERE name LIKE '%{term}%'").fetchall()

+rows = db.execute("SELECT id, name, email FROM users WHERE name LIKE ", ()).fetchall()

```

---

### SQLI-003 — 🔴 CRITICAL

**File:** `app.py` · **Line:** 82  
**Pattern:** `string_concat` · **Classifier confidence:** 100%

**Vulnerable code:**
```python
query = "DELETE FROM " + table + " WHERE id = " + record_id
```

**Explanation:**
> Replaced string concatenation SQL with parameterized query. User-supplied variables ['table', 'record_id'] are now bound safely via '?' placeholders.

**Patched code:**
```python
db.execute("DELETE FROM  ?", (table, record_id))
```

**Diff:**
```diff
--- a/app.py:82
+++ b/app.py:82
@@ -1 +1 @@
-query = "DELETE FROM " + table + " WHERE id = " + record_id

+db.execute("DELETE FROM  ?", (table, record_id))

```

---